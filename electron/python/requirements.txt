# Core dependencies - use minimum versions to get latest compatible releases
# spotdl will pull in all its required dependencies automatically
#
# NOTE: yt-dlp is a rolling-release package that breaks whenever YouTube changes
# things server-side. setup_venv.sh upgrades it to the latest release on every
# launch, so this floor is only a hard minimum for a first install, not the
# version users actually run day-to-day.
spotdl>=4.5.2
ytmusicapi>=1.10.0
yt-dlp>=2026.7.4
